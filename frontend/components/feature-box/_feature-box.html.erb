<div class="feature-box <%= with_background ? 'with-background' : '' %> <%= defined?(_custom_class) ? _custom_class : '' %>" style='<%= with_background ? "background-image: url('#{asset_pack_path("media/shared/static/#{_background_image}")}')" : "" %>'>
  <%= yield %>
</div>
